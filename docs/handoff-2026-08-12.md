# Handoff: Create the Cascadia Curiosities repo

**For:** a Claude Code session rooted in `C:\Projects\cascadia-curiosities` (create the folder first, start the session there — NOT in cascadia-standards; hooks load only from the session's primary working directory)
**From:** Cowork session, 2026-08-12
**Author:** Aaron Robbins. Aaron does not write code and does not run scripts; he approves decisions, the agent executes. Ambiguity stops the work and asks. Never end with "now run this."

## Objective

Create a new repo, **Cascadia Curiosities** (`cascadia-curiosities`), as the permanent home for non-BI work built to the Cascadia standard. First resident: the sibling-conflict-combinatorics paper, currently sitting untracked in `C:\Projects\cascadia-standards\submissions\sibling-conflict-intelligencer\`. Queried to The Mathematical Intelligencer 2026-08-12 (co-EiCs Parshall and Tabachnikov); reply pending.

## Decisions already made by Aaron (do not re-ask)

1. Name: **Cascadia Curiosities** / `cascadia-curiosities`.
2. Structure: **per-piece folders**, each self-contained with its own `governance/`, mirroring the module-repo pattern.
3. Scope: **full trail migrates** — manuscript, figures, build scripts, governance records, research record, drafts, correspondence.
4. Cleanup: after verified copy, the source folder is removed from cascadia-standards (it is untracked there; no commit to cascadia-standards is needed or permitted from your session).
5. Visibility: **match Aaron's other repos.** He believes they are public but wants this verified — check the visibility of his existing GitHub repos (e.g., the cascadia module repos / RobbinsAnalytics) with `gh repo view --json visibility` or the web UI, report what you find, and create `cascadia-curiosities` at the same visibility **after confirming with Aaron**.

## Target structure

```
cascadia-curiosities/
  README.md                      ← what this repo is (see seed notes below)
  CLAUDE.md                      ← repo conventions (see requirements below)
  .claude/                       ← starter kit; copy from cascadia-standards (it is the canonical home
                                   of the .claude starter kit — settings.json deny-list for git add -A/.,
                                   PreToolUse whitespace hook)
  curiosities/
    sibling-conflict-combinatorics/
      manuscript/                ← manuscript.tex, manuscript.pdf, Robbins-Combinatorics-of-Sibling-Conflict.pdf
      figures/                   ← figure PDFs, PNGs (design + narrow), build_*.py scripts
      governance/                ← chart-review.md, panel-returns.md, pre-panel-notes.md
      research/                  ← RESEARCH-RECORD.md, original-handoff.md, draft-revised.md
      correspondence/            ← query-email-2026-08-12.md (append future replies here)
```

Source mapping from `C:\Projects\cascadia-standards\submissions\sibling-conflict-intelligencer\`:
- `manuscript.tex`, `manuscript.pdf`, `Robbins-Combinatorics-of-Sibling-Conflict.pdf` → `manuscript/`
- `figure*.pdf`, `figure*.png`, `build_*.py` → `figures/`
- `chart-review.md`, `panel-returns.md`, `pre-panel-notes.md` → `governance/`
- `research/*` → `research/`
- `correspondence/*` → `correspondence/`
- This handoff file → keep a copy at repo root as `docs/handoff-2026-08-12.md` or delete after completion, Aaron's call.

## CLAUDE.md requirements for the new repo

Derive from the module-repo template in cascadia-standards (`templates/`), adjusted:
- cascadia-standards is **read-only reference** from this repo (VIZ-PRINCIPLES.md and CHART-REVIEW.md live there; currently v2.5; both ship together under one version number).
- Stage by name, never `git add -A` or `git add .` (enforced by the copied settings.json).
- Every chart in this repo clears CHART-REVIEW.md including the Rule 7.4 reading panel before shipping.
- Aaron does not run scripts; the agent executes.
- Note the paper's status field (query sent 2026-08-12 → update as it progresses) so future sessions don't re-submit or double-post.
- The Control Tower company-name allow-list does NOT govern this repo (no Cascadia company universe here), but the calibration-filer rule is absolute everywhere: that company is never named in any Robbins repo.

## Verification before cleanup

1. Copy the tree, then verify: file count and checksums match between source and destination (`sha256sum` both sides, diff the lists).
2. Recompile `manuscript.tex` in the new location (pdflatex ×2) and confirm a 7-page PDF builds — proves the figure paths survived the move.
3. Initial commit: stage by name, per-folder. Confirm the whitespace hook is active before the first commit (`.claude/` copied and loaded).
4. Only after 1–3 pass: delete `C:\Projects\cascadia-standards\submissions\` (the whole folder — it contains only this piece and is untracked; confirm with `git -C C:\Projects\cascadia-standards status` that nothing tracked changes).

## Out of scope for this handoff

- Linking the paper from robbinsanalytics.com (site repo work; separate session rooted there). Preprint posting is permitted by Springer's policy — verified 2026-08-12 — label it "preprint, under consideration at The Mathematical Intelligencer" and disclose the posting at formal SNAPP submission.
- Any response to the editors. If a reply has arrived by the time this runs, log it in `correspondence/` and stop; drafting the response is Aaron's call in a fresh conversation.
- JHM fallback variant (anonymized, abstract, their reference style) — only if the Intelligencer declines.

## Open items carried from the build

- Panel v2's N metric is unmeasured (no pre-panel list was recorded before the second panel run). Recorded honestly in chart-review.md; do not reconstruct it.
- Panel dispositions (both rounds) are marked "proposed, awaiting Aaron's ratification" in chart-review.md. Aaron has functionally ratified by approving the shipped figures and sending the query; a future session may update the header line to say so if Aaron confirms.
